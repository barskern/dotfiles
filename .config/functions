# Change the directory to a subject folder
subs() {
	DOC_DIR="$(xdg-user-dir DOCUMENTS)"
	NTNU_DIR="$DOC_DIR/ntnu"
	choice=$(\
		fd '^\w{3}\d{4}$' --type d --max-depth 1 "$NTNU_DIR" \
		| xargs basename -a \
		| fzf \
	)
	cd "$NTNU_DIR/$choice"
}
